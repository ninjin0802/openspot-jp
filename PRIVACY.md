# Privacy

OpenSpot JP requests foreground coarse or precise location only after the user presses the current-location button. It does not request background location, retain location history, or include analytics SDKs.

Coordinates used for a nearby search are sent to the configured OpenSpot JP API. The application and Worker must not log those coordinates. Favorites remain in the app's local Room database.
